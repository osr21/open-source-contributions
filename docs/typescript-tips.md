# TypeScript Tips

## Utility Types
Partial, Required, Readonly, Pick, Omit, Record, Exclude, Extract, ReturnType, Parameters

## Discriminated Unions
Use a shared literal "kind" field to safely narrow types.

## Template Literal Types
Build precise string types at the type level.

## satisfies operator
Validate a value against a type without widening it.

## const assertion
as const — freezes inferred types to literal values.
